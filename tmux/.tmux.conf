# Prefix: Ctrl+a (easier than default Ctrl+b)
unbind C-b
set -g prefix C-a
bind C-a send-prefix

# Split panes with | and -
bind | split-window -h -c "#{pane_current_path}"
bind - split-window -v -c "#{pane_current_path}"
unbind '"'
unbind %

# Switch panes with Alt+arrow (no prefix needed)
bind -n M-Left  select-pane -L
bind -n M-Right select-pane -R
bind -n M-Up    select-pane -U
bind -n M-Down  select-pane -D

# Resize panes with Ctrl+arrow
bind -n C-Left  resize-pane -L 5
bind -n C-Right resize-pane -R 5

# Mouse support
set -g mouse on
# Allow terminal (not tmux) to handle mouse selection for copy
unbind -T root MouseDrag1Pane
unbind -T copy-mode MouseDrag1Pane

# Start windows and panes at 1
set -g base-index 1
set -g pane-base-index 1

# Don't rename windows automatically
set-option -g allow-rename off

# 256 colors + true color
set -g default-terminal "tmux-256color"
set -ga terminal-overrides ",xterm-256color:Tc:RGB"

# Increase scrollback buffer
set -g history-limit 10000

# No delay on Escape key (important for vim/neovim)
set -sg escape-time 0

# Activity monitoring (flags sessions with new output)
set -g monitor-activity on
set -g visual-activity off

# Bell: audible + visual flash, propagate across all windows/sessions
set -g bell-action any
set -g monitor-bell on
set -g visual-bell both
set -g window-status-bell-style "fg=red,bold,blink"

# Session switcher with rich preview
bind s choose-tree -Zs -F "#{b:pane_current_path} | #{pane_title} [#{pane_current_command}] #{?session_attached,#[fg=green]▶#[default],  }"

# Vi-style copy mode
set -g mode-keys vi
bind -T copy-mode-vi v send -X begin-selection
bind -T copy-mode-vi y send -X copy-selection-and-cancel

# Pane gaps and full borders
set -g pane-border-lines heavy
set -g pane-border-style "fg=#626880"
set -g pane-active-border-style "fg=#8caaee"
set -gq pane-padding 1

# Plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-battery'
set -g @plugin 'catppuccin/tmux'

# Pre-set battery module text so catppuccin's `set -ogq` won't override them.
# Catppuccin's battery.conf wraps values in #{l:...} (literal), which prevents
# tmux from expanding #{battery_percentage}. Calling the scripts directly fixes this.
set -g @catppuccin_battery_icon "#(~/.tmux/plugins/tmux-battery/scripts/battery_icon.sh) "
set -g @catppuccin_battery_text " #(~/.tmux/plugins/tmux-battery/scripts/battery_percentage.sh)%"

set -g @catppuccin_flavor 'macchiato'
set -g @catppuccin_window_status_style "rounded"
set -g @catppuccin_date_time_text " %I:%M%p %d-%b"

# Status bar
set -g status-left "#{E:@catppuccin_status_directory}#{E:@catppuccin_status_gitmux}"
set -g status-right "#{E:@catppuccin_status_session}#{E:@catppuccin_status_date_time}#{E:@catppuccin_status_battery}"
set -g status-left-length 100
set -g status-right-length 100
set -g status-interval 5

run '~/.tmux/plugins/tpm/tpm'

# Override after plugin load: plugin uses set -gq (not -ogq) for _text so it
# overwrites any pre-run value. Setting here ensures our command wins.
set -g @catppuccin_gitmux_text " #(cd #{pane_current_path} && git branch --show-current 2>/dev/null)"
